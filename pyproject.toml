[project]
name = "scribe"
version = "2.0.0"
description = "Download, transcribe, and summarize university lecture recordings"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper>=1.2.1",
    "google-genai>=2.4.0",
    "python-dotenv>=1.0.0",
    "requests>=2.32.0",
    "tqdm>=4.67.0",
    "jdatetime>=5.0.0",
]
